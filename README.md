# Basic and advanced CPrograms
A repo to store programs covering different topics.

## COMPILE and RUN
### To compile: 
> gcc <file_name>.c 

This will create a `a.out` file in the same directory, where the command is run

### To run:
> ./a.out

